extends ../layout/index

block contenido 
    div(class="pb-10")
        h1(class="text-4xl my-10 font-extrabold text-center text-purple-500") Real
            span.font-normal.text-black Estate
        h2(class="text-center text-2xl font-bold")= page

        p.text-xl.font-bold.text-center.my-10= message
        if(ok)
            a(href="auth/login" class="text-center text-2x1 font-bold") now login with your new passoword 